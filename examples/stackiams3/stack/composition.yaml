# Generated by tf2crossplane stack — StackIAMS3
# Do not edit manually — re-run tf2crossplane stack to regenerate.
apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: xstackiams3s.homelab.crossplane.io
spec:
  compositeTypeRef:
    apiVersion: homelab.crossplane.io/v1alpha1
    kind: XStackIAMS3
  mode: Pipeline
  pipeline:
  - step: render
    functionRef:
      name: function-go-templating
    input:
      apiVersion: gotemplating.fn.crossplane.io/v1beta1
      kind: GoTemplate
      source: Inline
      inline:
        template: "{{- if and (not .observed.composite.resource.spec.iam.existingId)\
          \ (not .observed.composite.resource.spec.iam.import) }}\n---\napiVersion:\
          \ homelab.crossplane.io/v1alpha1\nkind: XIamRole\nmetadata:\n  name: {{\
          \ .observed.composite.resource.metadata.name }}-iam\n  namespace: {{ .observed.composite.resource.metadata.namespace\
          \ }}\n  annotations:\n    gotemplating.fn.crossplane.io/composition-resource-name:\
          \ iam\nspec:\n  providerConfig: {{ .observed.composite.resource.spec.providerConfig\
          \ }}\n{{- if .observed.composite.resource.spec.iam.name }}\n  name: {{ .observed.composite.resource.spec.iam.name\
          \ }}\n{{- end }}\n{{- if .observed.composite.resource.spec.iam.tags }}\n\
          \  tags:{{ .observed.composite.resource.spec.iam.tags | toYaml | nindent\
          \ 4 }}\n{{- end }}\n{{- if .observed.composite.resource.spec.iam.create_instance_profile\
          \ }}\n  create_instance_profile: {{ .observed.composite.resource.spec.iam.create_instance_profile\
          \ }}\n{{- end }}\n{{- if .observed.composite.resource.spec.iam.create_inline_policy\
          \ }}\n  create_inline_policy: {{ .observed.composite.resource.spec.iam.create_inline_policy\
          \ }}\n{{- end }}\n{{- if .observed.composite.resource.spec.iam.inline_policy_permissions\
          \ }}\n  inline_policy_permissions:{{ .observed.composite.resource.spec.iam.inline_policy_permissions\
          \ | toYaml | nindent 4 }}\n{{- end }}\n{{- if .observed.composite.resource.spec.iam.trust_policy_permissions\
          \ }}\n  trust_policy_permissions:{{ .observed.composite.resource.spec.iam.trust_policy_permissions\
          \ | toYaml | nindent 4 }}\n{{- end }}\n{{- else if .observed.composite.resource.spec.iam.import\
          \ }}\n---\napiVersion: homelab.crossplane.io/v1alpha1\nkind: XIamRole\n\
          metadata:\n  name: {{ .observed.composite.resource.metadata.name }}-iam\n\
          \  namespace: {{ .observed.composite.resource.metadata.namespace }}\n  annotations:\n\
          \    gotemplating.fn.crossplane.io/composition-resource-name: iam\nspec:\n\
          \  providerConfig: {{ .observed.composite.resource.spec.providerConfig }}\n\
          \  import: {{ .observed.composite.resource.spec.iam.import }}\n{{- if .observed.composite.resource.spec.iam.name\
          \ }}\n  name: {{ .observed.composite.resource.spec.iam.name }}\n{{- end\
          \ }}\n{{- if .observed.composite.resource.spec.iam.tags }}\n  tags:{{ .observed.composite.resource.spec.iam.tags\
          \ | toYaml | nindent 4 }}\n{{- end }}\n{{- if .observed.composite.resource.spec.iam.create_instance_profile\
          \ }}\n  create_instance_profile: {{ .observed.composite.resource.spec.iam.create_instance_profile\
          \ }}\n{{- end }}\n{{- if .observed.composite.resource.spec.iam.create_inline_policy\
          \ }}\n  create_inline_policy: {{ .observed.composite.resource.spec.iam.create_inline_policy\
          \ }}\n{{- end }}\n{{- if .observed.composite.resource.spec.iam.inline_policy_permissions\
          \ }}\n  inline_policy_permissions:{{ .observed.composite.resource.spec.iam.inline_policy_permissions\
          \ | toYaml | nindent 4 }}\n{{- end }}\n{{- if .observed.composite.resource.spec.iam.trust_policy_permissions\
          \ }}\n  trust_policy_permissions:{{ .observed.composite.resource.spec.iam.trust_policy_permissions\
          \ | toYaml | nindent 4 }}\n{{- end }}\n{{- end }}\n---\napiVersion: homelab.crossplane.io/v1alpha1\n\
          kind: XS3Bucket\nmetadata:\n  name: {{ .observed.composite.resource.metadata.name\
          \ }}-s3\n  namespace: {{ .observed.composite.resource.metadata.namespace\
          \ }}\n  annotations:\n    gotemplating.fn.crossplane.io/composition-resource-name:\
          \ s3\nspec:\n  providerConfig: {{ .observed.composite.resource.spec.providerConfig\
          \ }}\n{{- if .observed.composite.resource.spec.s3.bucket }}\n  bucket: {{\
          \ .observed.composite.resource.spec.s3.bucket }}\n{{- end }}\n{{- if .observed.composite.resource.spec.s3.force_destroy\
          \ }}\n  force_destroy: {{ .observed.composite.resource.spec.s3.force_destroy\
          \ }}\n{{- end }}\n{{- if .observed.composite.resource.spec.s3.tags }}\n\
          \  tags:{{ .observed.composite.resource.spec.s3.tags | toYaml | nindent\
          \ 4 }}\n{{- end }}"
  - step: auto-ready
    functionRef:
      name: function-auto-ready
