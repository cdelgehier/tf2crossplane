# Generated by tf2crossplane infra from git::https://github.com/terraform-aws-modules/terraform-aws-iam.git//modules/iam-role?ref=v6.4.0
# Do not edit manually — re-run tf2crossplane infra to regenerate.
apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: xiamroles.homelab.crossplane.io
spec:
  compositeTypeRef:
    apiVersion: homelab.crossplane.io/v1alpha1
    kind: XIamRole
  mode: Pipeline
  pipeline:
  - step: render
    functionRef:
      name: function-go-templating
    input:
      apiVersion: gotemplating.fn.crossplane.io/v1beta1
      kind: GoTemplate
      source: Inline
      inline:
        template: |
          apiVersion: opentofu.m.upbound.io/v1beta1
          kind: Workspace
          metadata:
            name: {{ .observed.composite.resource.metadata.name }}
            annotations:
              gotemplating.fn.crossplane.io/composition-resource-name: workspace
          spec:
            providerConfigRef:
              name: {{ .observed.composite.resource.spec.providerConfig }}
              kind: ProviderConfig
            forProvider:
              source: Remote
              module: git::https://github.com/terraform-aws-modules/terraform-aws-iam.git//modules/iam-role?ref=v6.4.0
              varmap:
                {{- if hasKey .observed.composite.resource.spec "create" }}
                create: {{ index .observed.composite.resource.spec "create" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "tags" }}
                tags: {{ index .observed.composite.resource.spec "tags" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "name" }}
                name: {{ index .observed.composite.resource.spec "name" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "use_name_prefix" }}
                use_name_prefix: {{ index .observed.composite.resource.spec "use_name_prefix" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "path" }}
                path: {{ index .observed.composite.resource.spec "path" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "description" }}
                description: {{ index .observed.composite.resource.spec "description" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "max_session_duration" }}
                max_session_duration: {{ index .observed.composite.resource.spec "max_session_duration" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "permissions_boundary" }}
                permissions_boundary: {{ index .observed.composite.resource.spec "permissions_boundary" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "trust_policy_permissions" }}
                trust_policy_permissions: {{ index .observed.composite.resource.spec "trust_policy_permissions" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "trust_policy_conditions" }}
                trust_policy_conditions: {{ index .observed.composite.resource.spec "trust_policy_conditions" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "policies" }}
                policies: {{ index .observed.composite.resource.spec "policies" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "enable_oidc" }}
                enable_oidc: {{ index .observed.composite.resource.spec "enable_oidc" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "oidc_account_id" }}
                oidc_account_id: {{ index .observed.composite.resource.spec "oidc_account_id" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "oidc_provider_urls" }}
                oidc_provider_urls: {{ index .observed.composite.resource.spec "oidc_provider_urls" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "oidc_subjects" }}
                oidc_subjects: {{ index .observed.composite.resource.spec "oidc_subjects" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "oidc_wildcard_subjects" }}
                oidc_wildcard_subjects: {{ index .observed.composite.resource.spec "oidc_wildcard_subjects" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "oidc_audiences" }}
                oidc_audiences: {{ index .observed.composite.resource.spec "oidc_audiences" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "enable_github_oidc" }}
                enable_github_oidc: {{ index .observed.composite.resource.spec "enable_github_oidc" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "github_provider" }}
                github_provider: {{ index .observed.composite.resource.spec "github_provider" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "enable_bitbucket_oidc" }}
                enable_bitbucket_oidc: {{ index .observed.composite.resource.spec "enable_bitbucket_oidc" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "enable_saml" }}
                enable_saml: {{ index .observed.composite.resource.spec "enable_saml" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "saml_provider_ids" }}
                saml_provider_ids: {{ index .observed.composite.resource.spec "saml_provider_ids" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "saml_endpoints" }}
                saml_endpoints: {{ index .observed.composite.resource.spec "saml_endpoints" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "saml_trust_actions" }}
                saml_trust_actions: {{ index .observed.composite.resource.spec "saml_trust_actions" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "create_inline_policy" }}
                create_inline_policy: {{ index .observed.composite.resource.spec "create_inline_policy" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "source_inline_policy_documents" }}
                source_inline_policy_documents: {{ index .observed.composite.resource.spec "source_inline_policy_documents" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "override_inline_policy_documents" }}
                override_inline_policy_documents: {{ index .observed.composite.resource.spec "override_inline_policy_documents" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "inline_policy_permissions" }}
                inline_policy_permissions: {{ index .observed.composite.resource.spec "inline_policy_permissions" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "create_instance_profile" }}
                create_instance_profile: {{ index .observed.composite.resource.spec "create_instance_profile" }}
                {{- end }}
  - step: patch-outputs
    functionRef:
      name: function-patch-and-transform
    input:
      apiVersion: pt.fn.crossplane.io/v1beta1
      kind: Resources
      resources:
      - name: workspace
        patches:
        - type: ToCompositeFieldPath
          fromFieldPath: status.atProvider.outputs.name
          toFieldPath: status.atProvider.outputs.name
          policy:
            fromFieldPath: Optional
        - type: ToCompositeFieldPath
          fromFieldPath: status.atProvider.outputs.arn
          toFieldPath: status.atProvider.outputs.arn
          policy:
            fromFieldPath: Optional
        - type: ToCompositeFieldPath
          fromFieldPath: status.atProvider.outputs.unique_id
          toFieldPath: status.atProvider.outputs.unique_id
          policy:
            fromFieldPath: Optional
        - type: ToCompositeFieldPath
          fromFieldPath: status.atProvider.outputs.instance_profile_arn
          toFieldPath: status.atProvider.outputs.instance_profile_arn
          policy:
            fromFieldPath: Optional
        - type: ToCompositeFieldPath
          fromFieldPath: status.atProvider.outputs.instance_profile_id
          toFieldPath: status.atProvider.outputs.instance_profile_id
          policy:
            fromFieldPath: Optional
        - type: ToCompositeFieldPath
          fromFieldPath: status.atProvider.outputs.instance_profile_name
          toFieldPath: status.atProvider.outputs.instance_profile_name
          policy:
            fromFieldPath: Optional
        - type: ToCompositeFieldPath
          fromFieldPath: status.atProvider.outputs.instance_profile_unique_id
          toFieldPath: status.atProvider.outputs.instance_profile_unique_id
          policy:
            fromFieldPath: Optional
  - step: auto-ready
    functionRef:
      name: function-auto-ready
