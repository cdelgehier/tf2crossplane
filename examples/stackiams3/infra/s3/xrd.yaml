# Generated by tf2crossplane infra from git::https://github.com/terraform-aws-modules/terraform-aws-s3-bucket.git?ref=v4.6.0
# Do not edit manually — re-run tf2crossplane infra to regenerate.
apiVersion: apiextensions.crossplane.io/v2
kind: CompositeResourceDefinition
metadata:
  name: xs3buckets.homelab.crossplane.io
spec:
  scope: Namespaced
  group: homelab.crossplane.io
  names:
    kind: XS3Bucket
    plural: xs3buckets
  defaultCompositionRef:
    name: xs3buckets.homelab.crossplane.io
  defaultCompositionUpdatePolicy: Automatic
  versions:
  - name: v1alpha1
    served: true
    referenceable: true
    schema:
      openAPIV3Schema:
        type: object
        properties:
          spec:
            type: object
            properties:
              providerConfig:
                type: string
                description: ProviderConfig to use (e.g. my-provider-config, my-other-provider-config)
              create_bucket:
                type: boolean
                description: Controls if S3 bucket should be created
              attach_elb_log_delivery_policy:
                type: boolean
                description: Controls if S3 bucket should have ELB log delivery policy
                  attached
              attach_lb_log_delivery_policy:
                type: boolean
                description: Controls if S3 bucket should have ALB/NLB log delivery
                  policy attached
              attach_access_log_delivery_policy:
                type: boolean
                description: Controls if S3 bucket should have S3 access log delivery
                  policy attached
              attach_deny_insecure_transport_policy:
                type: boolean
                description: Controls if S3 bucket should have deny non-SSL transport
                  policy attached
              attach_require_latest_tls_policy:
                type: boolean
                description: Controls if S3 bucket should require the latest version
                  of TLS
              attach_policy:
                type: boolean
                description: Controls if S3 bucket should have bucket policy attached
                  (set to `true` to use value of `policy` as bucket policy)
              attach_public_policy:
                type: boolean
                description: Controls if a user defined public bucket policy will
                  be attached (set to `false` to allow upstream to apply defaults
                  to the bucket)
              attach_inventory_destination_policy:
                type: boolean
                description: Controls if S3 bucket should have bucket inventory destination
                  policy attached.
              attach_analytics_destination_policy:
                type: boolean
                description: Controls if S3 bucket should have bucket analytics destination
                  policy attached.
              attach_deny_incorrect_encryption_headers:
                type: boolean
                description: Controls if S3 bucket should deny incorrect encryption
                  headers policy attached.
              attach_deny_incorrect_kms_key_sse:
                type: boolean
                description: Controls if S3 bucket policy should deny usage of incorrect
                  KMS key SSE.
              allowed_kms_key_arn:
                type: string
                description: The ARN of KMS key which should be allowed in PutObject
              attach_deny_unencrypted_object_uploads:
                type: boolean
                description: Controls if S3 bucket should deny unencrypted object
                  uploads policy attached.
              attach_deny_ssec_encrypted_object_uploads:
                type: boolean
                description: Controls if S3 bucket should deny SSEC encrypted object
                  uploads.
              bucket:
                type: string
                description: (Optional, Forces new resource) The name of the bucket.
                  If omitted, Terraform will assign a random, unique name.
              bucket_prefix:
                type: string
                description: (Optional, Forces new resource) Creates a unique bucket
                  name beginning with the specified prefix. Conflicts with bucket.
              acl:
                type: string
                description: (Optional) The canned ACL to apply. Conflicts with `grant`
              policy:
                type: string
                description: (Optional) A valid bucket policy JSON document. Note
                  that if the policy document is not specific enough (but still valid),
                  Terraform may view the policy as constantly changing in a terraform
                  plan. In this case, please make sure you use the verbose/specific
                  version of the policy. For more information about building AWS IAM
                  policy documents with Terraform, see the AWS IAM Policy Document
                  Guide.
              tags:
                type: object
                additionalProperties:
                  type: string
                description: (Optional) A mapping of tags to assign to the bucket.
              force_destroy:
                type: boolean
                description: (Optional, Default:false ) A boolean that indicates all
                  objects should be deleted from the bucket so that the bucket can
                  be destroyed without error. These objects are not recoverable.
              acceleration_status:
                type: string
                description: (Optional) Sets the accelerate configuration of an existing
                  bucket. Can be Enabled or Suspended.
              request_payer:
                type: string
                description: (Optional) Specifies who should bear the cost of Amazon
                  S3 data transfer. Can be either BucketOwner or Requester. By default,
                  the owner of the S3 bucket would incur the costs of any data transfer.
                  See Requester Pays Buckets developer guide for more information.
              website:
                type: object
                x-kubernetes-preserve-unknown-fields: true
                description: Map containing static web-site hosting or redirect configuration.
              cors_rule:
                type: array
                items:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                description: List of maps containing rules for Cross-Origin Resource
                  Sharing.
              versioning:
                type: object
                additionalProperties:
                  type: string
                description: Map containing versioning configuration.
              logging:
                type: object
                x-kubernetes-preserve-unknown-fields: true
                description: Map containing access bucket logging configuration.
              access_log_delivery_policy_source_buckets:
                type: array
                items:
                  type: string
                description: (Optional) List of S3 bucket ARNs which should be allowed
                  to deliver access logs to this bucket.
              access_log_delivery_policy_source_accounts:
                type: array
                items:
                  type: string
                description: (Optional) List of AWS Account IDs should be allowed
                  to deliver access logs to this bucket.
              grant:
                type: array
                items:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                description: An ACL policy grant. Conflicts with `acl`
              owner:
                type: object
                additionalProperties:
                  type: string
                description: Bucket owner's display name and ID. Conflicts with `acl`
              expected_bucket_owner:
                type: string
                description: The account ID of the expected bucket owner
              transition_default_minimum_object_size:
                type: string
                description: 'The default minimum object size behavior applied to
                  the lifecycle configuration. Valid values: all_storage_classes_128K
                  (default), varies_by_storage_class'
              lifecycle_rule:
                type: array
                items:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                description: List of maps containing configuration of object lifecycle
                  management.
              replication_configuration:
                type: object
                x-kubernetes-preserve-unknown-fields: true
                description: Map containing cross-region replication configuration.
              server_side_encryption_configuration:
                type: object
                x-kubernetes-preserve-unknown-fields: true
                description: Map containing server-side encryption configuration.
              intelligent_tiering:
                type: object
                x-kubernetes-preserve-unknown-fields: true
                description: Map containing intelligent tiering configuration.
              object_lock_configuration:
                type: object
                x-kubernetes-preserve-unknown-fields: true
                description: Map containing S3 object locking configuration.
              metric_configuration:
                type: array
                items:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                description: Map containing bucket metric configuration.
              inventory_configuration:
                type: object
                x-kubernetes-preserve-unknown-fields: true
                description: Map containing S3 inventory configuration.
              inventory_source_account_id:
                type: string
                description: The inventory source account id.
              inventory_source_bucket_arn:
                type: string
                description: The inventory source bucket ARN.
              inventory_self_source_destination:
                type: boolean
                description: Whether or not the inventory source bucket is also the
                  destination bucket.
              analytics_configuration:
                type: object
                x-kubernetes-preserve-unknown-fields: true
                description: Map containing bucket analytics configuration.
              analytics_source_account_id:
                type: string
                description: The analytics source account id.
              analytics_source_bucket_arn:
                type: string
                description: The analytics source bucket ARN.
              analytics_self_source_destination:
                type: boolean
                description: Whether or not the analytics source bucket is also the
                  destination bucket.
              object_lock_enabled:
                type: boolean
                description: Whether S3 bucket should have an Object Lock configuration
                  enabled.
              block_public_acls:
                type: boolean
                description: Whether Amazon S3 should block public ACLs for this bucket.
              block_public_policy:
                type: boolean
                description: Whether Amazon S3 should block public bucket policies
                  for this bucket.
              ignore_public_acls:
                type: boolean
                description: Whether Amazon S3 should ignore public ACLs for this
                  bucket.
              restrict_public_buckets:
                type: boolean
                description: Whether Amazon S3 should restrict public bucket policies
                  for this bucket.
              control_object_ownership:
                type: boolean
                description: Whether to manage S3 Bucket Ownership Controls on this
                  bucket.
              object_ownership:
                type: string
                description: 'Object ownership. Valid values: BucketOwnerEnforced,
                  BucketOwnerPreferred or ObjectWriter. ''BucketOwnerEnforced'': ACLs
                  are disabled, and the bucket owner automatically owns and has full
                  control over every object in the bucket. ''BucketOwnerPreferred'':
                  Objects uploaded to the bucket change ownership to the bucket owner
                  if the objects are uploaded with the bucket-owner-full-control canned
                  ACL. ''ObjectWriter'': The uploading account will own the object
                  if the object is uploaded with the bucket-owner-full-control canned
                  ACL.'
              is_directory_bucket:
                type: boolean
                description: If the s3 bucket created is a directory bucket
              data_redundancy:
                type: string
                description: 'Data redundancy. Valid values: `SingleAvailabilityZone`'
              type:
                type: string
                description: 'Bucket type. Valid values: `Directory`'
              availability_zone_id:
                type: string
                description: Availability Zone ID or Local Zone ID
              location_type:
                type: string
                description: 'Location type. Valid values: `AvailabilityZone` or `LocalZone`'
              putin_khuylo:
                type: boolean
                description: 'Do you agree that Putin doesn''t respect Ukrainian sovereignty
                  and territorial integrity? More info: https://en.wikipedia.org/wiki/Putin_khuylo!'
            required:
            - providerConfig
          status:
            type: object
            properties:
              atProvider:
                type: object
                properties:
                  outputs:
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                    properties:
                      s3_bucket_id:
                        type: string
                        description: The name of the bucket.
                      s3_bucket_arn:
                        type: string
                        description: The ARN of the bucket. Will be of format arn:aws:s3:::bucketname.
                      s3_bucket_bucket_domain_name:
                        type: string
                        description: The bucket domain name. Will be of format bucketname.s3.amazonaws.com.
                      s3_bucket_bucket_regional_domain_name:
                        type: string
                        description: 'The bucket region-specific domain name. The
                          bucket domain name including the region name, please refer
                          here for format. Note: The AWS CloudFront allows specifying
                          S3 region-specific endpoint when creating S3 origin, it
                          will prevent redirect issues from CloudFront to S3 Origin
                          URL.'
                      s3_bucket_hosted_zone_id:
                        type: string
                        description: The Route 53 Hosted Zone ID for this bucket's
                          region.
                      s3_bucket_lifecycle_configuration_rules:
                        type: string
                        description: The lifecycle rules of the bucket, if the bucket
                          is configured with lifecycle rules. If not, this will be
                          an empty string.
                      s3_bucket_policy:
                        type: string
                        description: The policy of the bucket, if the bucket is configured
                          with a policy. If not, this will be an empty string.
                      s3_bucket_region:
                        type: string
                        description: The AWS region this bucket resides in.
                      s3_bucket_website_endpoint:
                        type: string
                        description: The website endpoint, if the bucket is configured
                          with a website. If not, this will be an empty string.
                      s3_bucket_website_domain:
                        type: string
                        description: The domain of the website endpoint, if the bucket
                          is configured with a website. If not, this will be an empty
                          string. This is used to create Route 53 alias records.
                      s3_directory_bucket_name:
                        type: string
                        description: Name of the directory bucket.
                      s3_directory_bucket_arn:
                        type: string
                        description: ARN of the directory bucket.
