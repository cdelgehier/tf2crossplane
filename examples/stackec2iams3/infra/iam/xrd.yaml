# Generated by tf2crossplane infra from git::https://github.com/terraform-aws-modules/terraform-aws-iam.git//modules/iam-role?ref=v6.4.0
# Do not edit manually — re-run tf2crossplane infra to regenerate.
apiVersion: apiextensions.crossplane.io/v2
kind: CompositeResourceDefinition
metadata:
  name: xiamroles.homelab.crossplane.io
spec:
  scope: Namespaced
  group: homelab.crossplane.io
  names:
    kind: XIamRole
    plural: xiamroles
  defaultCompositionRef:
    name: xiamroles.homelab.crossplane.io
  defaultCompositionUpdatePolicy: Automatic
  versions:
  - name: v1alpha1
    served: true
    referenceable: true
    schema:
      openAPIV3Schema:
        type: object
        properties:
          spec:
            type: object
            properties:
              providerConfig:
                type: string
                description: ProviderConfig to use (e.g. my-provider-config, my-other-provider-config)
              create:
                type: boolean
                description: Controls if resources should be created (affects all
                  resources)
              tags:
                type: object
                additionalProperties:
                  type: string
                description: A map of tags to add to all resources
              name:
                type: string
                description: Name to use on IAM role created
              use_name_prefix:
                type: boolean
                description: Determines whether the IAM role name (`name`) is used
                  as a prefix
              path:
                type: string
                description: Path of IAM role
              description:
                type: string
                description: Description of the role
              max_session_duration:
                type: number
                description: Maximum session duration (in seconds) that you want to
                  set for the specified role. If you do not specify a value for this
                  setting, the default maximum of one hour is applied. This setting
                  can have a value from 1 hour to 12 hours
              permissions_boundary:
                type: string
                description: ARN of the policy that is used to set the permissions
                  boundary for the IAM role
              trust_policy_permissions:
                type: object
                additionalProperties:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                description: A map of IAM policy [statements](https://registry.terraform.io/providers/hashicorp/aws/latest/docs/data-sources/iam_policy_document#statement)
                  for custom trust policy permissions
              trust_policy_conditions:
                type: array
                items:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                description: '[Condition constraints](https://registry.terraform.io/providers/hashicorp/aws/latest/docs/data-sources/iam_policy_document#condition)
                  applied to the trust policy(s) enabled'
              policies:
                type: object
                additionalProperties:
                  type: string
                description: Policies to attach to the IAM role in `{'static_name'
                  = 'policy_arn'}` format
              enable_oidc:
                type: boolean
                description: Enable OIDC provider trust for the role
              oidc_account_id:
                type: string
                description: An overriding AWS account ID where the OIDC provider
                  lives; leave empty to use the current account ID for the AWS provider
              oidc_provider_urls:
                type: array
                items:
                  type: string
                description: List of URLs of the OIDC Providers
              oidc_subjects:
                type: array
                items:
                  type: string
                description: The fully qualified OIDC subjects to be added to the
                  role policy
              oidc_wildcard_subjects:
                type: array
                items:
                  type: string
                description: The OIDC subject using wildcards to be added to the role
                  policy
              oidc_audiences:
                type: array
                items:
                  type: string
                description: The audience to be added to the role policy. Set to sts.amazonaws.com
                  for cross-account assumable role. Leave empty otherwise.
              enable_github_oidc:
                type: boolean
                description: Enable GitHub OIDC provider trust for the role
              github_provider:
                type: string
                description: The GitHub OIDC provider URL *without the `https://`
                  prefix
              enable_bitbucket_oidc:
                type: boolean
                description: Enable Bitbucket OIDC provider trust for the role
              enable_saml:
                type: boolean
                description: Enable SAML provider trust for the role
              saml_provider_ids:
                type: array
                items:
                  type: string
                description: List of SAML provider IDs
              saml_endpoints:
                type: array
                items:
                  type: string
                description: List of AWS SAML endpoints
              saml_trust_actions:
                type: array
                items:
                  type: string
                description: Additional assume role trust actions for the SAML federated
                  statement
              create_inline_policy:
                type: boolean
                description: Determines whether to create an inline policy
              source_inline_policy_documents:
                type: array
                items:
                  type: string
                description: List of IAM policy documents that are merged together
                  into the exported document. Statements must have unique `sid`s
              override_inline_policy_documents:
                type: array
                items:
                  type: string
                description: List of IAM policy documents that are merged together
                  into the exported document. In merging, statements with non-blank
                  `sid`s will override statements with the same `sid`
              inline_policy_permissions:
                type: object
                additionalProperties:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                description: A map of IAM policy [statements](https://registry.terraform.io/providers/hashicorp/aws/latest/docs/data-sources/iam_policy_document#statement)
                  for inline policy permissions
              create_instance_profile:
                type: boolean
                description: Determines whether to create an instance profile
            required:
            - providerConfig
          status:
            type: object
            properties:
              outputs:
                type: object
                x-kubernetes-preserve-unknown-fields: true
