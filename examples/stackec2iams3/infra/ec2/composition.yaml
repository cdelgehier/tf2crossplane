# Generated by tf2crossplane infra from git::https://github.com/terraform-aws-modules/terraform-aws-ec2-instance.git?ref=v6.3.0
# Do not edit manually — re-run tf2crossplane infra to regenerate.
apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: xec2instances.homelab.crossplane.io
spec:
  compositeTypeRef:
    apiVersion: homelab.crossplane.io/v1alpha1
    kind: XEC2Instance
  mode: Pipeline
  pipeline:
  - step: render
    functionRef:
      name: function-go-templating
    input:
      apiVersion: gotemplating.fn.crossplane.io/v1beta1
      kind: GoTemplate
      source: Inline
      inline:
        template: |
          apiVersion: opentofu.m.upbound.io/v1beta1
          kind: Workspace
          metadata:
            name: {{ .observed.composite.resource.metadata.name }}
            annotations:
              gotemplating.fn.crossplane.io/composition-resource-name: workspace
          spec:
            providerConfigRef:
              name: {{ .observed.composite.resource.spec.providerConfig }}
              kind: ProviderConfig
            forProvider:
              source: Remote
              module: git::https://github.com/terraform-aws-modules/terraform-aws-ec2-instance.git?ref=v6.3.0
              varmap:
                {{- if hasKey .observed.composite.resource.spec "create" }}
                create: {{ index .observed.composite.resource.spec "create" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "name" }}
                name: {{ index .observed.composite.resource.spec "name" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "region" }}
                region: {{ index .observed.composite.resource.spec "region" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "ami" }}
                ami: {{ index .observed.composite.resource.spec "ami" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "ami_ssm_parameter" }}
                ami_ssm_parameter: {{ index .observed.composite.resource.spec "ami_ssm_parameter" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "ignore_ami_changes" }}
                ignore_ami_changes: {{ index .observed.composite.resource.spec "ignore_ami_changes" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "associate_public_ip_address" }}
                associate_public_ip_address: {{ index .observed.composite.resource.spec "associate_public_ip_address" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "availability_zone" }}
                availability_zone: {{ index .observed.composite.resource.spec "availability_zone" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "capacity_reservation_specification" }}
                capacity_reservation_specification: {{ index .observed.composite.resource.spec "capacity_reservation_specification" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "cpu_options" }}
                cpu_options: {{ index .observed.composite.resource.spec "cpu_options" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "cpu_credits" }}
                cpu_credits: {{ index .observed.composite.resource.spec "cpu_credits" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "disable_api_termination" }}
                disable_api_termination: {{ index .observed.composite.resource.spec "disable_api_termination" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "disable_api_stop" }}
                disable_api_stop: {{ index .observed.composite.resource.spec "disable_api_stop" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "ebs_optimized" }}
                ebs_optimized: {{ index .observed.composite.resource.spec "ebs_optimized" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "enclave_options_enabled" }}
                enclave_options_enabled: {{ index .observed.composite.resource.spec "enclave_options_enabled" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "enable_primary_ipv6" }}
                enable_primary_ipv6: {{ index .observed.composite.resource.spec "enable_primary_ipv6" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "ephemeral_block_device" }}
                ephemeral_block_device: {{ index .observed.composite.resource.spec "ephemeral_block_device" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "get_password_data" }}
                get_password_data: {{ index .observed.composite.resource.spec "get_password_data" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "hibernation" }}
                hibernation: {{ index .observed.composite.resource.spec "hibernation" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "host_id" }}
                host_id: {{ index .observed.composite.resource.spec "host_id" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "host_resource_group_arn" }}
                host_resource_group_arn: {{ index .observed.composite.resource.spec "host_resource_group_arn" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "iam_instance_profile" }}
                iam_instance_profile: {{ index .observed.composite.resource.spec "iam_instance_profile" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "instance_initiated_shutdown_behavior" }}
                instance_initiated_shutdown_behavior: {{ index .observed.composite.resource.spec "instance_initiated_shutdown_behavior" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "instance_market_options" }}
                instance_market_options: {{ index .observed.composite.resource.spec "instance_market_options" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "instance_type" }}
                instance_type: {{ index .observed.composite.resource.spec "instance_type" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "ipv6_address_count" }}
                ipv6_address_count: {{ index .observed.composite.resource.spec "ipv6_address_count" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "ipv6_addresses" }}
                ipv6_addresses: {{ index .observed.composite.resource.spec "ipv6_addresses" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "key_name" }}
                key_name: {{ index .observed.composite.resource.spec "key_name" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "launch_template" }}
                launch_template: {{ index .observed.composite.resource.spec "launch_template" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "maintenance_options" }}
                maintenance_options: {{ index .observed.composite.resource.spec "maintenance_options" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "metadata_options" }}
                metadata_options: {{ index .observed.composite.resource.spec "metadata_options" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "monitoring" }}
                monitoring: {{ index .observed.composite.resource.spec "monitoring" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "network_interface" }}
                network_interface: {{ index .observed.composite.resource.spec "network_interface" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "placement_group" }}
                placement_group: {{ index .observed.composite.resource.spec "placement_group" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "placement_partition_number" }}
                placement_partition_number: {{ index .observed.composite.resource.spec "placement_partition_number" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "private_dns_name_options" }}
                private_dns_name_options: {{ index .observed.composite.resource.spec "private_dns_name_options" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "private_ip" }}
                private_ip: {{ index .observed.composite.resource.spec "private_ip" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "root_block_device" }}
                root_block_device: {{ index .observed.composite.resource.spec "root_block_device" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "secondary_private_ips" }}
                secondary_private_ips: {{ index .observed.composite.resource.spec "secondary_private_ips" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "source_dest_check" }}
                source_dest_check: {{ index .observed.composite.resource.spec "source_dest_check" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "subnet_id" }}
                subnet_id: {{ index .observed.composite.resource.spec "subnet_id" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "tags" }}
                tags: {{ index .observed.composite.resource.spec "tags" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "instance_tags" }}
                instance_tags: {{ index .observed.composite.resource.spec "instance_tags" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "tenancy" }}
                tenancy: {{ index .observed.composite.resource.spec "tenancy" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "user_data" }}
                user_data: {{ index .observed.composite.resource.spec "user_data" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "user_data_base64" }}
                user_data_base64: {{ index .observed.composite.resource.spec "user_data_base64" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "user_data_replace_on_change" }}
                user_data_replace_on_change: {{ index .observed.composite.resource.spec "user_data_replace_on_change" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "volume_tags" }}
                volume_tags: {{ index .observed.composite.resource.spec "volume_tags" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "enable_volume_tags" }}
                enable_volume_tags: {{ index .observed.composite.resource.spec "enable_volume_tags" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "vpc_security_group_ids" }}
                vpc_security_group_ids: {{ index .observed.composite.resource.spec "vpc_security_group_ids" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "timeouts" }}
                timeouts: {{ index .observed.composite.resource.spec "timeouts" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "create_spot_instance" }}
                create_spot_instance: {{ index .observed.composite.resource.spec "create_spot_instance" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "spot_instance_interruption_behavior" }}
                spot_instance_interruption_behavior: {{ index .observed.composite.resource.spec "spot_instance_interruption_behavior" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "spot_launch_group" }}
                spot_launch_group: {{ index .observed.composite.resource.spec "spot_launch_group" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "spot_price" }}
                spot_price: {{ index .observed.composite.resource.spec "spot_price" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "spot_type" }}
                spot_type: {{ index .observed.composite.resource.spec "spot_type" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "spot_wait_for_fulfillment" }}
                spot_wait_for_fulfillment: {{ index .observed.composite.resource.spec "spot_wait_for_fulfillment" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "spot_valid_from" }}
                spot_valid_from: {{ index .observed.composite.resource.spec "spot_valid_from" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "spot_valid_until" }}
                spot_valid_until: {{ index .observed.composite.resource.spec "spot_valid_until" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "ebs_volumes" }}
                ebs_volumes: {{ index .observed.composite.resource.spec "ebs_volumes" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "create_iam_instance_profile" }}
                create_iam_instance_profile: {{ index .observed.composite.resource.spec "create_iam_instance_profile" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "iam_role_name" }}
                iam_role_name: {{ index .observed.composite.resource.spec "iam_role_name" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "iam_role_use_name_prefix" }}
                iam_role_use_name_prefix: {{ index .observed.composite.resource.spec "iam_role_use_name_prefix" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "iam_role_path" }}
                iam_role_path: {{ index .observed.composite.resource.spec "iam_role_path" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "iam_role_description" }}
                iam_role_description: {{ index .observed.composite.resource.spec "iam_role_description" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "iam_role_permissions_boundary" }}
                iam_role_permissions_boundary: {{ index .observed.composite.resource.spec "iam_role_permissions_boundary" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "iam_role_policies" }}
                iam_role_policies: {{ index .observed.composite.resource.spec "iam_role_policies" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "iam_role_tags" }}
                iam_role_tags: {{ index .observed.composite.resource.spec "iam_role_tags" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "create_security_group" }}
                create_security_group: {{ index .observed.composite.resource.spec "create_security_group" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "security_group_name" }}
                security_group_name: {{ index .observed.composite.resource.spec "security_group_name" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "security_group_use_name_prefix" }}
                security_group_use_name_prefix: {{ index .observed.composite.resource.spec "security_group_use_name_prefix" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "security_group_description" }}
                security_group_description: {{ index .observed.composite.resource.spec "security_group_description" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "security_group_vpc_id" }}
                security_group_vpc_id: {{ index .observed.composite.resource.spec "security_group_vpc_id" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "security_group_tags" }}
                security_group_tags: {{ index .observed.composite.resource.spec "security_group_tags" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "security_group_egress_rules" }}
                security_group_egress_rules: {{ index .observed.composite.resource.spec "security_group_egress_rules" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "security_group_ingress_rules" }}
                security_group_ingress_rules: {{ index .observed.composite.resource.spec "security_group_ingress_rules" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "create_eip" }}
                create_eip: {{ index .observed.composite.resource.spec "create_eip" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "eip_domain" }}
                eip_domain: {{ index .observed.composite.resource.spec "eip_domain" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "eip_tags" }}
                eip_tags: {{ index .observed.composite.resource.spec "eip_tags" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "putin_khuylo" }}
                putin_khuylo: {{ index .observed.composite.resource.spec "putin_khuylo" }}
                {{- end }}
  - step: auto-ready
    functionRef:
      name: function-auto-ready
