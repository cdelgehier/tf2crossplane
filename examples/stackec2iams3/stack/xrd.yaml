# Generated by tf2crossplane stack — StackEC2IAMS3
# Do not edit manually — re-run tf2crossplane stack to regenerate.
apiVersion: apiextensions.crossplane.io/v2
kind: CompositeResourceDefinition
metadata:
  name: xstackec2iams3s.homelab.crossplane.io
spec:
  group: homelab.crossplane.io
  scope: Namespaced
  names:
    kind: XStackEC2IAMS3
    plural: xstackec2iams3s
  defaultCompositionRef:
    name: xstackec2iams3s.homelab.crossplane.io
  versions:
  - name: v1alpha1
    served: true
    referenceable: true
    schema:
      openAPIV3Schema:
        type: object
        properties:
          spec:
            type: object
            required:
            - providerConfig
            properties:
              providerConfig:
                type: string
                description: Name of the ProviderConfig to use for all composed resources.
              ec2:
                type: object
                properties:
                  ami_ssm_parameter:
                    type: string
                    description: SSM parameter name for the AMI ID. For Amazon Linux
                      AMI SSM parameters see [reference](https://docs.aws.amazon.com/systems-manager/latest/userguide/parameter-store-public-parameters-ami.html)
                  instance_type:
                    type: string
                    description: The type of instance to start
                  subnet_id:
                    type: string
                    description: The VPC Subnet ID to launch in
                  tags:
                    type: object
                    additionalProperties:
                      type: string
                    description: A mapping of tags to assign to the resource
              iam:
                type: object
                properties:
                  tags:
                    type: object
                    additionalProperties:
                      type: string
                    description: A map of tags to add to all resources
                  name:
                    type: string
                    description: Name to use on IAM role created
                  trust_policy_permissions:
                    type: object
                    additionalProperties:
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    description: A map of IAM policy [statements](https://registry.terraform.io/providers/hashicorp/aws/latest/docs/data-sources/iam_policy_document#statement)
                      for custom trust policy permissions
                  create_inline_policy:
                    type: boolean
                    description: Determines whether to create an inline policy
                  inline_policy_permissions:
                    type: object
                    additionalProperties:
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    description: A map of IAM policy [statements](https://registry.terraform.io/providers/hashicorp/aws/latest/docs/data-sources/iam_policy_document#statement)
                      for inline policy permissions
                  create_instance_profile:
                    type: boolean
                    description: Determines whether to create an instance profile
                  existingId:
                    type: string
                    description: Cloud-native identifier of a pre-existing iam (ARN,
                      resource ID…). When set no resource is created; the identifier
                      is forwarded to dependent resources.
                  import:
                    type: string
                    description: Cloud-native identifier of a pre-existing iam to
                      adopt. Crossplane will manage it going forward.
          status:
            type: object
            properties:
              iamInstanceProfileName:
                type: string
                description: 'Resolved output: iam.outputs.instance_profile_name'
