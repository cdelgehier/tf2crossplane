# Generated by tf2crossplane infra from git::https://github.com/terraform-aws-modules/terraform-aws-ec2-instance.git?ref=v6.3.0
# Do not edit manually — re-run tf2crossplane infra to regenerate.
apiVersion: apiextensions.crossplane.io/v2
kind: CompositeResourceDefinition
metadata:
  name: xec2instances.homelab.crossplane.io
spec:
  scope: Namespaced
  group: homelab.crossplane.io
  names:
    kind: XEC2Instance
    plural: xec2instances
  defaultCompositionRef:
    name: xec2instances.homelab.crossplane.io
  defaultCompositionUpdatePolicy: Automatic
  versions:
  - name: v1alpha1
    served: true
    referenceable: true
    schema:
      openAPIV3Schema:
        type: object
        properties:
          spec:
            type: object
            properties:
              providerConfig:
                type: string
                description: ProviderConfig to use (e.g. my-provider-config, my-other-provider-config)
              create:
                type: boolean
                description: Whether to create an instance
              name:
                type: string
                description: Name to be used on EC2 instance created
              region:
                type: string
                description: Region where the resource(s) will be managed. Defaults
                  to the Region set in the provider configuration
              ami:
                type: string
                description: ID of AMI to use for the instance
              ami_ssm_parameter:
                type: string
                description: SSM parameter name for the AMI ID. For Amazon Linux AMI
                  SSM parameters see [reference](https://docs.aws.amazon.com/systems-manager/latest/userguide/parameter-store-public-parameters-ami.html)
              ignore_ami_changes:
                type: boolean
                description: Whether changes to the AMI ID changes should be ignored
                  by Terraform. Note - changing this value will result in the replacement
                  of the instance
              associate_public_ip_address:
                type: boolean
                description: Whether to associate a public IP address with an instance
                  in a VPC
              availability_zone:
                type: string
                description: AZ to start the instance in
              capacity_reservation_specification:
                type: object
                x-kubernetes-preserve-unknown-fields: true
                description: Describes an instance's Capacity Reservation targeting
                  option
              cpu_options:
                type: object
                x-kubernetes-preserve-unknown-fields: true
                description: Defines CPU options to apply to the instance at launch
                  time.
              cpu_credits:
                type: string
                description: The credit option for CPU usage (unlimited or standard)
              disable_api_termination:
                type: boolean
                description: If true, enables EC2 Instance Termination Protection
              disable_api_stop:
                type: boolean
                description: If true, enables EC2 Instance Stop Protection
              ebs_optimized:
                type: boolean
                description: If true, the launched EC2 instance will be EBS-optimized
              enclave_options_enabled:
                type: boolean
                description: Whether Nitro Enclaves will be enabled on the instance.
                  Defaults to `false`
              enable_primary_ipv6:
                type: boolean
                description: Whether to assign a primary IPv6 Global Unicast Address
                  (GUA) to the instance when launched in a dual-stack or IPv6-only
                  subnet
              ephemeral_block_device:
                type: object
                additionalProperties:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                description: Customize Ephemeral (also known as Instance Store) volumes
                  on the instance
              get_password_data:
                type: boolean
                description: If true, wait for password data to become available and
                  retrieve it
              hibernation:
                type: boolean
                description: If true, the launched EC2 instance will support hibernation
              host_id:
                type: string
                description: ID of a dedicated host that the instance will be assigned
                  to. Use when an instance is to be launched on a specific dedicated
                  host
              host_resource_group_arn:
                type: string
                description: ARN of the host resource group in which to launch the
                  instances. If you specify an ARN, omit the `tenancy` parameter or
                  set it to `host`
              iam_instance_profile:
                type: string
                description: IAM Instance Profile to launch the instance with. Specified
                  as the name of the Instance Profile
              instance_initiated_shutdown_behavior:
                type: string
                description: Shutdown behavior for the instance. Amazon defaults this
                  to stop for EBS-backed instances and terminate for instance-store
                  instances. Cannot be set on instance-store instance
              instance_market_options:
                type: object
                x-kubernetes-preserve-unknown-fields: true
                description: The market (purchasing) option for the instance. If set,
                  overrides the `create_spot_instance` variable
              instance_type:
                type: string
                description: The type of instance to start
              ipv6_address_count:
                type: number
                description: A number of IPv6 addresses to associate with the primary
                  network interface. Amazon EC2 chooses the IPv6 addresses from the
                  range of your subnet
              ipv6_addresses:
                type: array
                items:
                  type: string
                description: Specify one or more IPv6 addresses from the range of
                  the subnet to associate with the primary network interface
              key_name:
                type: string
                description: Key name of the Key Pair to use for the instance; which
                  can be managed using the `aws_key_pair` resource
              launch_template:
                type: object
                x-kubernetes-preserve-unknown-fields: true
                description: Specifies a Launch Template to configure the instance.
                  Parameters configured on this resource will override the corresponding
                  parameters in the Launch Template
              maintenance_options:
                type: object
                x-kubernetes-preserve-unknown-fields: true
                description: The maintenance options for the instance
              metadata_options:
                type: object
                x-kubernetes-preserve-unknown-fields: true
                description: Customize the metadata options of the instance
              monitoring:
                type: boolean
                description: If true, the launched EC2 instance will have detailed
                  monitoring enabled
              network_interface:
                type: object
                additionalProperties:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                description: Customize network interfaces to be attached at instance
                  boot time
              placement_group:
                type: string
                description: The Placement Group to start the instance in
              placement_partition_number:
                type: number
                description: Number of the partition the instance is in. Valid only
                  if the `aws_placement_group` resource's `strategy` argument is set
                  to `partition`
              private_dns_name_options:
                type: object
                x-kubernetes-preserve-unknown-fields: true
                description: Customize the private DNS name options of the instance
              private_ip:
                type: string
                description: Private IP address to associate with the instance in
                  a VPC
              root_block_device:
                type: object
                x-kubernetes-preserve-unknown-fields: true
                description: Customize details about the root block device of the
                  instance. See Block Devices below for details
              secondary_private_ips:
                type: array
                items:
                  type: string
                description: A list of secondary private IPv4 addresses to assign
                  to the instance's primary network interface (eth0) in a VPC. Can
                  only be assigned to the primary network interface (eth0) attached
                  at instance creation, not a pre-existing network interface i.e.
                  referenced in a `network_interface block`
              source_dest_check:
                type: boolean
                description: Controls if traffic is routed to the instance when the
                  destination address does not match the instance. Used for NAT or
                  VPNs
              subnet_id:
                type: string
                description: The VPC Subnet ID to launch in
              tags:
                type: object
                additionalProperties:
                  type: string
                description: A mapping of tags to assign to the resource
              instance_tags:
                type: object
                additionalProperties:
                  type: string
                description: Additional tags for the instance
              tenancy:
                type: string
                description: 'The tenancy of the instance (if the instance is running
                  in a VPC). Available values: default, dedicated, host'
              user_data:
                type: string
                description: The user data to provide when launching the instance.
                  Do not pass gzip-compressed data via this argument; see user_data_base64
                  instead
              user_data_base64:
                type: string
                description: Can be used instead of user_data to pass base64-encoded
                  binary data directly. Use this instead of user_data whenever the
                  value is not a valid UTF-8 string. For example, gzip-encoded user
                  data must be base64-encoded and passed via this argument to avoid
                  corruption
              user_data_replace_on_change:
                type: boolean
                description: When used in combination with user_data or user_data_base64
                  will trigger a destroy and recreate when set to true. Defaults to
                  false if not set
              volume_tags:
                type: object
                additionalProperties:
                  type: string
                description: A mapping of tags to assign to the devices created by
                  the instance at launch time
              enable_volume_tags:
                type: boolean
                description: Whether to enable volume tags (if enabled it conflicts
                  with root_block_device tags)
              vpc_security_group_ids:
                type: array
                items:
                  type: string
                description: A list of security group IDs to associate with
              timeouts:
                type: object
                x-kubernetes-preserve-unknown-fields: true
                description: Define maximum timeout for creating, updating, and deleting
                  EC2 instance resources
              create_spot_instance:
                type: boolean
                description: Depicts if the instance is a spot instance
              spot_instance_interruption_behavior:
                type: string
                description: Indicates Spot instance behavior when it is interrupted.
                  Valid values are `terminate`, `stop`, or `hibernate`
              spot_launch_group:
                type: string
                description: A launch group is a group of spot instances that launch
                  together and terminate together. If left empty instances are launched
                  and terminated individually
              spot_price:
                type: string
                description: The maximum price to request on the spot market. Defaults
                  to on-demand price
              spot_type:
                type: string
                description: If set to one-time, after the instance is terminated,
                  the spot request will be closed. Default `persistent`
              spot_wait_for_fulfillment:
                type: boolean
                description: If set, Terraform will wait for the Spot Request to be
                  fulfilled, and will throw an error if the timeout of 10m is reached
              spot_valid_from:
                type: string
                description: The start date and time of the request, in UTC RFC3339
                  format(for example, YYYY-MM-DDTHH:MM:SSZ)
              spot_valid_until:
                type: string
                description: The end date and time of the request, in UTC RFC3339
                  format(for example, YYYY-MM-DDTHH:MM:SSZ)
              ebs_volumes:
                type: object
                additionalProperties:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                description: Additional EBS volumes to attach to the instance
              create_iam_instance_profile:
                type: boolean
                description: Determines whether an IAM instance profile is created
                  or to use an existing IAM instance profile
              iam_role_name:
                type: string
                description: Name to use on IAM role created
              iam_role_use_name_prefix:
                type: boolean
                description: Determines whether the IAM role name (`iam_role_name`
                  or `name`) is used as a prefix
              iam_role_path:
                type: string
                description: IAM role path
              iam_role_description:
                type: string
                description: Description of the role
              iam_role_permissions_boundary:
                type: string
                description: ARN of the policy that is used to set the permissions
                  boundary for the IAM role
              iam_role_policies:
                type: object
                additionalProperties:
                  type: string
                description: Policies attached to the IAM role
              iam_role_tags:
                type: object
                additionalProperties:
                  type: string
                description: A map of additional tags to add to the IAM role/profile
                  created
              create_security_group:
                type: boolean
                description: Determines whether a security group will be created
              security_group_name:
                type: string
                description: Name to use on security group created
              security_group_use_name_prefix:
                type: boolean
                description: Determines whether the security group name (`security_group_name`
                  or `name`) is used as a prefix
              security_group_description:
                type: string
                description: Description of the security group
              security_group_vpc_id:
                type: string
                description: VPC ID to create the security group in. If not set, the
                  security group will be created in the default VPC
              security_group_tags:
                type: object
                additionalProperties:
                  type: string
                description: A map of additional tags to add to the security group
                  created
              security_group_egress_rules:
                type: object
                additionalProperties:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                description: Egress rules to add to the security group
              security_group_ingress_rules:
                type: object
                additionalProperties:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                description: Ingress rules to add to the security group
              create_eip:
                type: boolean
                description: Determines whether a public EIP will be created and associated
                  with the instance.
              eip_domain:
                type: string
                description: Indicates if this EIP is for use in VPC
              eip_tags:
                type: object
                additionalProperties:
                  type: string
                description: A map of additional tags to add to the eip
              putin_khuylo:
                type: boolean
                description: 'Do you agree that Putin doesn''t respect Ukrainian sovereignty
                  and territorial integrity? More info: https://en.wikipedia.org/wiki/Putin_khuylo!'
            required:
            - providerConfig
          status:
            type: object
            properties:
              atProvider:
                type: object
                properties:
                  outputs:
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                    properties:
                      id:
                        type: string
                        description: The ID of the instance
                      arn:
                        type: string
                        description: The ARN of the instance
                      capacity_reservation_specification:
                        type: string
                        description: Capacity reservation specification of the instance
                      instance_state:
                        type: string
                        description: The state of the instance
                      outpost_arn:
                        type: string
                        description: The ARN of the Outpost the instance is assigned
                          to
                      password_data:
                        type: string
                        description: Base-64 encoded encrypted password data for the
                          instance. Useful for getting the administrator password
                          for instances running Microsoft Windows. This attribute
                          is only exported if `get_password_data` is true
                      primary_network_interface_id:
                        type: string
                        description: The ID of the instance's primary network interface
                      private_dns:
                        type: string
                        description: The private DNS name assigned to the instance.
                          Can only be used inside the Amazon EC2, and only available
                          if you've enabled DNS hostnames for your VPC
                      public_dns:
                        type: string
                        description: The public DNS name assigned to the instance.
                          For EC2-VPC, this is only available if you've enabled DNS
                          hostnames for your VPC
                      public_ip:
                        type: string
                        description: The public IP address assigned to the instance,
                          if applicable.
                      private_ip:
                        type: string
                        description: The private IP address assigned to the instance
                      ipv6_addresses:
                        type: string
                        description: The IPv6 address assigned to the instance, if
                          applicable
                      tags_all:
                        x-kubernetes-preserve-unknown-fields: true
                        description: A map of tags assigned to the resource, including
                          those inherited from the provider default_tags configuration
                          block
                      spot_bid_status:
                        type: string
                        description: The current bid status of the Spot Instance Request
                      spot_request_state:
                        type: string
                        description: The current request state of the Spot Instance
                          Request
                      spot_instance_id:
                        type: string
                        description: The Instance ID (if any) that is currently fulfilling
                          the Spot Instance request
                      ami:
                        type: string
                        description: AMI ID that was used to create the instance
                      availability_zone:
                        type: string
                        description: The availability zone of the created instance
                      ebs_volumes:
                        x-kubernetes-preserve-unknown-fields: true
                        description: Map of EBS volumes created and their attributes
                      iam_role_name:
                        type: string
                        description: The name of the IAM role
                      iam_role_arn:
                        type: string
                        description: The Amazon Resource Name (ARN) specifying the
                          IAM role
                      iam_role_unique_id:
                        type: string
                        description: Stable and unique string identifying the IAM
                          role
                      iam_instance_profile_arn:
                        type: string
                        description: ARN assigned by AWS to the instance profile
                      iam_instance_profile_id:
                        type: string
                        description: Instance profile's ID
                      iam_instance_profile_unique:
                        type: string
                        description: Stable and unique string identifying the IAM
                          instance profile
                      root_block_device:
                        type: string
                        description: Root block device information
                      ebs_block_device:
                        type: string
                        description: EBS block device information
                      ephemeral_block_device:
                        type: string
                        description: Ephemeral block device information
                      security_group_arn:
                        type: string
                        description: Amazon Resource Name (ARN) of the security group
                      security_group_id:
                        type: string
                        description: ID of the security group
