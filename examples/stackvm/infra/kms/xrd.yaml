# Generated by tf2crossplane infra from git::https://github.com/terraform-aws-modules/terraform-aws-kms.git?ref=v4.2.0
# Do not edit manually — re-run tf2crossplane infra to regenerate.
apiVersion: apiextensions.crossplane.io/v2
kind: CompositeResourceDefinition
metadata:
  name: xkmss.homelab.crossplane.io
spec:
  scope: Namespaced
  group: homelab.crossplane.io
  names:
    kind: XKms
    plural: xkmss
  defaultCompositionRef:
    name: xkmss.homelab.crossplane.io
  defaultCompositionUpdatePolicy: Automatic
  versions:
  - name: v1alpha1
    served: true
    referenceable: true
    schema:
      openAPIV3Schema:
        type: object
        properties:
          spec:
            type: object
            properties:
              providerConfig:
                type: string
                description: ProviderConfig to use (e.g. my-provider-config, my-other-provider-config)
              create:
                type: boolean
                description: Determines whether resources will be created (affects
                  all resources)
              region:
                type: string
                description: Region where the resource(s) will be managed. Defaults
                  to the Region set in the provider configuration
              tags:
                type: object
                additionalProperties:
                  type: string
                description: A map of tags to add to all resources
              create_external:
                type: boolean
                description: Determines whether an external CMK (externally provided
                  material) will be created or a standard CMK (AWS provided material)
              bypass_policy_lockout_safety_check:
                type: boolean
                description: A flag to indicate whether to bypass the key policy lockout
                  safety check. Setting this value to true increases the risk that
                  the KMS key becomes unmanageable
              customer_master_key_spec:
                type: string
                description: 'Specifies whether the key contains a symmetric key or
                  an asymmetric key pair and the encryption algorithms or signing
                  algorithms that the key supports. Valid values: `SYMMETRIC_DEFAULT`,
                  `RSA_2048`, `RSA_3072`, `RSA_4096`, `HMAC_256`, `ECC_NIST_P256`,
                  `ECC_NIST_P384`, `ECC_NIST_P521`, or `ECC_SECG_P256K1`. Defaults
                  to `SYMMETRIC_DEFAULT`'
              custom_key_store_id:
                type: string
                description: ID of the KMS Custom Key Store where the key will be
                  stored instead of KMS (eg CloudHSM).
              deletion_window_in_days:
                type: number
                description: The waiting period, specified in number of days. After
                  the waiting period ends, AWS KMS deletes the KMS key. If you specify
                  a value, it must be between `7` and `30`, inclusive. If you do not
                  specify a value, it defaults to `30`
              description:
                type: string
                description: The description of the key as viewed in AWS console
              enable_key_rotation:
                type: boolean
                description: Specifies whether key rotation is enabled. Defaults to
                  `true`
              is_enabled:
                type: boolean
                description: Specifies whether the key is enabled. Defaults to `true`
              key_material_base64:
                type: string
                description: Base64 encoded 256-bit symmetric encryption key material
                  to import. The CMK is permanently associated with this key material.
                  External key only
              key_usage:
                type: string
                description: 'Specifies the intended use of the key. Valid values:
                  `ENCRYPT_DECRYPT` or `SIGN_VERIFY`. Defaults to `ENCRYPT_DECRYPT`'
              multi_region:
                type: boolean
                description: Indicates whether the KMS key is a multi-Region (`true`)
                  or regional (`false`) key. Defaults to `false`
              policy:
                type: string
                description: A valid policy JSON document. Although this is a key
                  policy, not an IAM policy, an `aws_iam_policy_document`, in the
                  form that designates a principal, can be used
              valid_to:
                type: string
                description: Time at which the imported key material expires. When
                  the key material expires, AWS KMS deletes the key material and the
                  CMK becomes unusable. If not specified, key material does not expire
              enable_default_policy:
                type: boolean
                description: Specifies whether to enable the default key policy. Defaults
                  to `true`
              key_owners:
                type: array
                items:
                  type: string
                description: A list of IAM ARNs for those who will have full key permissions
                  (`kms:*`)
              key_administrators:
                type: array
                items:
                  type: string
                description: A list of IAM ARNs for [key administrators](https://docs.aws.amazon.com/kms/latest/developerguide/key-policy-default.html#key-policy-default-allow-administrators)
              key_users:
                type: array
                items:
                  type: string
                description: A list of IAM ARNs for [key users](https://docs.aws.amazon.com/kms/latest/developerguide/key-policy-default.html#key-policy-default-allow-users)
              key_service_users:
                type: array
                items:
                  type: string
                description: A list of IAM ARNs for [key service users](https://docs.aws.amazon.com/kms/latest/developerguide/key-policy-default.html#key-policy-service-integration)
              key_service_roles_for_autoscaling:
                type: array
                items:
                  type: string
                description: A list of IAM ARNs for [AWSServiceRoleForAutoScaling
                  roles](https://docs.aws.amazon.com/autoscaling/ec2/userguide/key-policy-requirements-EBS-encryption.html#policy-example-cmk-access)
              key_symmetric_encryption_users:
                type: array
                items:
                  type: string
                description: A list of IAM ARNs for [key symmetric encryption users](https://docs.aws.amazon.com/kms/latest/developerguide/key-policy-default.html#key-policy-users-crypto)
              key_hmac_users:
                type: array
                items:
                  type: string
                description: A list of IAM ARNs for [key HMAC users](https://docs.aws.amazon.com/kms/latest/developerguide/key-policy-default.html#key-policy-users-crypto)
              key_asymmetric_public_encryption_users:
                type: array
                items:
                  type: string
                description: A list of IAM ARNs for [key asymmetric public encryption
                  users](https://docs.aws.amazon.com/kms/latest/developerguide/key-policy-default.html#key-policy-users-crypto)
              key_asymmetric_sign_verify_users:
                type: array
                items:
                  type: string
                description: A list of IAM ARNs for [key asymmetric sign and verify
                  users](https://docs.aws.amazon.com/kms/latest/developerguide/key-policy-default.html#key-policy-users-crypto)
              key_statements:
                type: array
                items:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                description: A map of IAM policy [statements](https://registry.terraform.io/providers/hashicorp/aws/latest/docs/data-sources/iam_policy_document#statement)
                  for custom permission usage
              key_spec:
                type: string
                description: 'Specifies whether the key contains a symmetric key or
                  an asymmetric key pair and the encryption algorithms or signing
                  algorithms that the key supports. Valid values: SYMMETRIC_DEFAULT,
                  RSA_2048, RSA_3072, RSA_4096, HMAC_224, HMAC_256, HMAC_384, HMAC_512,
                  ECC_NIST_P256, ECC_NIST_P384, ECC_NIST_P521, ECC_SECG_P256K1, ML_DSA_44,
                  ML_DSA_65, ML_DSA_87, or SM2 (China Regions only). Defaults to SYMMETRIC_DEFAULT'
              source_policy_documents:
                type: array
                items:
                  type: string
                description: List of IAM policy documents that are merged together
                  into the exported document. Statements must have unique `sid`s
              override_policy_documents:
                type: array
                items:
                  type: string
                description: List of IAM policy documents that are merged together
                  into the exported document. In merging, statements with non-blank
                  `sid`s will override statements with the same `sid`
              enable_route53_dnssec:
                type: boolean
                description: Determines whether the KMS policy used for Route53 DNSSEC
                  signing is enabled
              route53_dnssec_sources:
                type: array
                items:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                description: A list of maps containing `account_ids` and Route53 `hosted_zone_arn`
                  that will be allowed to sign DNSSEC records
              rotation_period_in_days:
                type: number
                description: Custom period of time between each rotation date. Must
                  be a number between 90 and 2560 (inclusive)
              create_replica:
                type: boolean
                description: Determines whether a replica standard CMK will be created
                  (AWS provided material)
              primary_key_arn:
                type: string
                description: The primary key arn of a multi-region replica key
              create_replica_external:
                type: boolean
                description: Determines whether a replica external CMK will be created
                  (externally provided material)
              primary_external_key_arn:
                type: string
                description: The primary external key arn of a multi-region replica
                  external key
              aliases:
                type: array
                items:
                  type: string
                description: A list of aliases to create. Note - due to the use of
                  `toset()`, values must be static strings and not computed values
              computed_aliases:
                type: object
                additionalProperties:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                description: A map of aliases to create. Values provided via the `name`
                  key of the map can be computed from upstream resources
              aliases_use_name_prefix:
                type: boolean
                description: Determines whether the alias name is used as a prefix
              grants:
                type: object
                additionalProperties:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                description: A map of grant definitions to create
            required:
            - providerConfig
          status:
            type: object
            properties:
              atProvider:
                type: object
                properties:
                  outputs:
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                    properties:
                      key_arn:
                        type: string
                        description: The Amazon Resource Name (ARN) of the key
                      key_id:
                        type: string
                        description: The globally unique identifier for the key
                      key_region:
                        type: string
                        description: The region for the key
                      key_policy:
                        x-kubernetes-preserve-unknown-fields: true
                        description: The IAM resource policy set on the key
                      external_key_expiration_model:
                        type: string
                        description: Whether the key material expires. Empty when
                          pending key material import, otherwise `KEY_MATERIAL_EXPIRES`
                          or `KEY_MATERIAL_DOES_NOT_EXPIRE`
                      external_key_state:
                        type: string
                        description: The state of the CMK
                      external_key_usage:
                        type: string
                        description: The cryptographic operations for which you can
                          use the CMK
                      aliases:
                        x-kubernetes-preserve-unknown-fields: true
                        description: A map of aliases created and their attributes
                      grants:
                        x-kubernetes-preserve-unknown-fields: true
                        description: A map of grants created and their attributes
