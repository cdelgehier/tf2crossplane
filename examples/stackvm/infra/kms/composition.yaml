# Generated by tf2crossplane infra from git::https://github.com/terraform-aws-modules/terraform-aws-kms.git?ref=v4.2.0
# Do not edit manually — re-run tf2crossplane infra to regenerate.
apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: xkmss.homelab.crossplane.io
spec:
  compositeTypeRef:
    apiVersion: homelab.crossplane.io/v1alpha1
    kind: XKms
  mode: Pipeline
  pipeline:
  - step: render
    functionRef:
      name: function-go-templating
    input:
      apiVersion: gotemplating.fn.crossplane.io/v1beta1
      kind: GoTemplate
      source: Inline
      inline:
        template: |
          apiVersion: opentofu.m.upbound.io/v1beta1
          kind: Workspace
          metadata:
            name: {{ .observed.composite.resource.metadata.name }}
            annotations:
              gotemplating.fn.crossplane.io/composition-resource-name: workspace
          spec:
            providerConfigRef:
              name: {{ .observed.composite.resource.spec.providerConfig }}
              kind: ProviderConfig
            forProvider:
              source: Remote
              module: git::https://github.com/terraform-aws-modules/terraform-aws-kms.git?ref=v4.2.0
              varmap:
                {{- if hasKey .observed.composite.resource.spec "create" }}
                create: {{ index .observed.composite.resource.spec "create" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "region" }}
                region: {{ index .observed.composite.resource.spec "region" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "tags" }}
                tags: {{ index .observed.composite.resource.spec "tags" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "create_external" }}
                create_external: {{ index .observed.composite.resource.spec "create_external" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "bypass_policy_lockout_safety_check" }}
                bypass_policy_lockout_safety_check: {{ index .observed.composite.resource.spec "bypass_policy_lockout_safety_check" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "customer_master_key_spec" }}
                customer_master_key_spec: {{ index .observed.composite.resource.spec "customer_master_key_spec" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "custom_key_store_id" }}
                custom_key_store_id: {{ index .observed.composite.resource.spec "custom_key_store_id" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "deletion_window_in_days" }}
                deletion_window_in_days: {{ index .observed.composite.resource.spec "deletion_window_in_days" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "description" }}
                description: {{ index .observed.composite.resource.spec "description" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "enable_key_rotation" }}
                enable_key_rotation: {{ index .observed.composite.resource.spec "enable_key_rotation" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "is_enabled" }}
                is_enabled: {{ index .observed.composite.resource.spec "is_enabled" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "key_material_base64" }}
                key_material_base64: {{ index .observed.composite.resource.spec "key_material_base64" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "key_usage" }}
                key_usage: {{ index .observed.composite.resource.spec "key_usage" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "multi_region" }}
                multi_region: {{ index .observed.composite.resource.spec "multi_region" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "policy" }}
                policy: {{ index .observed.composite.resource.spec "policy" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "valid_to" }}
                valid_to: {{ index .observed.composite.resource.spec "valid_to" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "enable_default_policy" }}
                enable_default_policy: {{ index .observed.composite.resource.spec "enable_default_policy" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "key_owners" }}
                key_owners: {{ index .observed.composite.resource.spec "key_owners" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "key_administrators" }}
                key_administrators: {{ index .observed.composite.resource.spec "key_administrators" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "key_users" }}
                key_users: {{ index .observed.composite.resource.spec "key_users" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "key_service_users" }}
                key_service_users: {{ index .observed.composite.resource.spec "key_service_users" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "key_service_roles_for_autoscaling" }}
                key_service_roles_for_autoscaling: {{ index .observed.composite.resource.spec "key_service_roles_for_autoscaling" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "key_symmetric_encryption_users" }}
                key_symmetric_encryption_users: {{ index .observed.composite.resource.spec "key_symmetric_encryption_users" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "key_hmac_users" }}
                key_hmac_users: {{ index .observed.composite.resource.spec "key_hmac_users" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "key_asymmetric_public_encryption_users" }}
                key_asymmetric_public_encryption_users: {{ index .observed.composite.resource.spec "key_asymmetric_public_encryption_users" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "key_asymmetric_sign_verify_users" }}
                key_asymmetric_sign_verify_users: {{ index .observed.composite.resource.spec "key_asymmetric_sign_verify_users" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "key_statements" }}
                key_statements: {{ index .observed.composite.resource.spec "key_statements" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "key_spec" }}
                key_spec: {{ index .observed.composite.resource.spec "key_spec" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "source_policy_documents" }}
                source_policy_documents: {{ index .observed.composite.resource.spec "source_policy_documents" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "override_policy_documents" }}
                override_policy_documents: {{ index .observed.composite.resource.spec "override_policy_documents" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "enable_route53_dnssec" }}
                enable_route53_dnssec: {{ index .observed.composite.resource.spec "enable_route53_dnssec" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "route53_dnssec_sources" }}
                route53_dnssec_sources: {{ index .observed.composite.resource.spec "route53_dnssec_sources" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "rotation_period_in_days" }}
                rotation_period_in_days: {{ index .observed.composite.resource.spec "rotation_period_in_days" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "create_replica" }}
                create_replica: {{ index .observed.composite.resource.spec "create_replica" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "primary_key_arn" }}
                primary_key_arn: {{ index .observed.composite.resource.spec "primary_key_arn" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "create_replica_external" }}
                create_replica_external: {{ index .observed.composite.resource.spec "create_replica_external" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "primary_external_key_arn" }}
                primary_external_key_arn: {{ index .observed.composite.resource.spec "primary_external_key_arn" | quote }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "aliases" }}
                aliases: {{ index .observed.composite.resource.spec "aliases" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "computed_aliases" }}
                computed_aliases: {{ index .observed.composite.resource.spec "computed_aliases" | toJson }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "aliases_use_name_prefix" }}
                aliases_use_name_prefix: {{ index .observed.composite.resource.spec "aliases_use_name_prefix" }}
                {{- end }}
                {{- if hasKey .observed.composite.resource.spec "grants" }}
                grants: {{ index .observed.composite.resource.spec "grants" | toJson }}
                {{- end }}
  - step: patch-outputs
    functionRef:
      name: function-patch-and-transform
    input:
      apiVersion: pt.fn.crossplane.io/v1beta1
      kind: Resources
      resources:
      - name: workspace
        patches:
        - type: ToCompositeFieldPath
          fromFieldPath: status.atProvider.outputs.key_arn
          toFieldPath: status.atProvider.outputs.key_arn
          policy:
            fromFieldPath: Optional
        - type: ToCompositeFieldPath
          fromFieldPath: status.atProvider.outputs.key_id
          toFieldPath: status.atProvider.outputs.key_id
          policy:
            fromFieldPath: Optional
        - type: ToCompositeFieldPath
          fromFieldPath: status.atProvider.outputs.key_region
          toFieldPath: status.atProvider.outputs.key_region
          policy:
            fromFieldPath: Optional
        - type: ToCompositeFieldPath
          fromFieldPath: status.atProvider.outputs.key_policy
          toFieldPath: status.atProvider.outputs.key_policy
          policy:
            fromFieldPath: Optional
        - type: ToCompositeFieldPath
          fromFieldPath: status.atProvider.outputs.external_key_expiration_model
          toFieldPath: status.atProvider.outputs.external_key_expiration_model
          policy:
            fromFieldPath: Optional
        - type: ToCompositeFieldPath
          fromFieldPath: status.atProvider.outputs.external_key_state
          toFieldPath: status.atProvider.outputs.external_key_state
          policy:
            fromFieldPath: Optional
        - type: ToCompositeFieldPath
          fromFieldPath: status.atProvider.outputs.external_key_usage
          toFieldPath: status.atProvider.outputs.external_key_usage
          policy:
            fromFieldPath: Optional
        - type: ToCompositeFieldPath
          fromFieldPath: status.atProvider.outputs.aliases
          toFieldPath: status.atProvider.outputs.aliases
          policy:
            fromFieldPath: Optional
        - type: ToCompositeFieldPath
          fromFieldPath: status.atProvider.outputs.grants
          toFieldPath: status.atProvider.outputs.grants
          policy:
            fromFieldPath: Optional
  - step: auto-ready
    functionRef:
      name: function-auto-ready
