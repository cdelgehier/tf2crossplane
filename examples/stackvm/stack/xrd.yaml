# Generated by tf2crossplane stack — StackVM
# Do not edit manually — re-run tf2crossplane stack to regenerate.
apiVersion: apiextensions.crossplane.io/v2
kind: CompositeResourceDefinition
metadata:
  name: xstackvms.homelab.crossplane.io
spec:
  group: homelab.crossplane.io
  scope: Namespaced
  names:
    kind: XStackVM
    plural: xstackvms
  defaultCompositionRef:
    name: xstackvms.homelab.crossplane.io
  versions:
  - name: v1alpha1
    served: true
    referenceable: true
    schema:
      openAPIV3Schema:
        type: object
        properties:
          spec:
            type: object
            required:
            - providerConfig
            properties:
              providerConfig:
                type: string
                description: Name of the ProviderConfig to use for all composed resources.
              kms:
                type: object
                properties:
                  create:
                    type: boolean
                    description: Determines whether resources will be created (affects
                      all resources)
                  tags:
                    type: object
                    additionalProperties:
                      type: string
                    description: A map of tags to add to all resources
                  deletion_window_in_days:
                    type: number
                    description: The waiting period, specified in number of days.
                      After the waiting period ends, AWS KMS deletes the KMS key.
                      If you specify a value, it must be between `7` and `30`, inclusive.
                      If you do not specify a value, it defaults to `30`
                  description:
                    type: string
                    description: The description of the key as viewed in AWS console
                  enable_key_rotation:
                    type: boolean
                    description: Specifies whether key rotation is enabled. Defaults
                      to `true`
                  existingId:
                    type: string
                    description: Cloud-native identifier of a pre-existing kms (ARN,
                      resource ID…). When set no resource is created; the identifier
                      is forwarded to dependent resources.
                  import:
                    type: string
                    description: Cloud-native identifier of a pre-existing kms to
                      adopt. Crossplane will manage it going forward.
              security_group:
                type: object
                properties:
                  vpc_id:
                    type: string
                    description: ID of the VPC where to create security group
                  name:
                    type: string
                    description: Name of security group - not required if create_sg
                      is false
                  description:
                    type: string
                    description: Description of security group
                  tags:
                    type: object
                    additionalProperties:
                      type: string
                    description: A mapping of tags to assign to security group
                  ingress_with_cidr_blocks:
                    type: array
                    items:
                      type: object
                      additionalProperties:
                        type: string
                    description: List of ingress rules to create where 'cidr_blocks'
                      is used
                  egress_with_cidr_blocks:
                    type: array
                    items:
                      type: object
                      additionalProperties:
                        type: string
                    description: List of egress rules to create where 'cidr_blocks'
                      is used
              ec2:
                type: object
                properties:
                  ami_ssm_parameter:
                    type: string
                    description: SSM parameter name for the AMI ID. For Amazon Linux
                      AMI SSM parameters see [reference](https://docs.aws.amazon.com/systems-manager/latest/userguide/parameter-store-public-parameters-ami.html)
                  instance_type:
                    type: string
                    description: The type of instance to start
                  subnet_id:
                    type: string
                    description: The VPC Subnet ID to launch in
                  tags:
                    type: object
                    additionalProperties:
                      type: string
                    description: A mapping of tags to assign to the resource
          status:
            type: object
            properties:
              kmsKeyArn:
                type: string
                description: 'Resolved output: kms.outputs.key_arn'
              securityGroupSecurityGroupId:
                type: string
                description: 'Resolved output: security_group.outputs.security_group_id'
